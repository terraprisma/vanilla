--- sources/TerrariaServerWindows/ReLogic/Localization/IME/FnaIme.cs
+++ sources/TerrariaServerLinux/ReLogic/Localization/IME/FnaIme.cs
@@ -1,3 +_,5 @@
+using Microsoft.Xna.Framework.Input;
+
 namespace ReLogic.Localization.IME;
 
 internal class FnaIme : PlatformIme
@@ -12,6 +_,11 @@
 
 	public override uint? SelectedCandidate => 0u;
 
+	public FnaIme()
+	{
+		TextInputEXT.TextInput += OnCharCallback;
+	}
+
 	private void OnCharCallback(char key)
 	{
 		if (base.IsEnabled)
@@ -27,16 +_,23 @@
 
 	protected override void OnEnable()
 	{
+		TextInputEXT.StartTextInput();
 	}
 
 	protected override void OnDisable()
 	{
+		TextInputEXT.StopTextInput();
 	}
 
 	protected override void Dispose(bool disposing)
 	{
 		if (!_disposedValue)
 		{
+			if (disposing)
+			{
+				TextInputEXT.TextInput -= OnCharCallback;
+				TextInputEXT.StopTextInput();
+			}
 			if (base.IsEnabled)
 			{
 				Disable();
