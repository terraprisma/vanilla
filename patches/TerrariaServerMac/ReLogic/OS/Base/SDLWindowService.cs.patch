--- sources/TerrariaServerWindows/ReLogic/OS/Base/SDLWindowService.cs
+++ sources/TerrariaServerMac/ReLogic/OS/Base/SDLWindowService.cs
@@ -1,5 +_,5 @@
-using System;
 using Microsoft.Xna.Framework;
+using SDL3;
 
 namespace ReLogic.OS.Base;
 
@@ -29,19 +_,30 @@
 
 	public void Activate(GameWindow window)
 	{
+		SDL.SDL_RaiseWindow(window.Handle);
 	}
 
 	public bool IsSizeable(GameWindow window)
 	{
-		throw new PlatformNotSupportedException();
+		SDL.SDL_WindowFlags sDL_WindowFlags = SDL.SDL_GetWindowFlags(window.Handle);
+		if ((sDL_WindowFlags & SDL.SDL_WindowFlags.SDL_WINDOW_RESIZABLE) != (SDL.SDL_WindowFlags)0uL)
+		{
+			return (sDL_WindowFlags & SDL.SDL_WindowFlags.SDL_WINDOW_BORDERLESS) == 0;
+		}
+		return false;
 	}
 
 	public void SetPosition(GameWindow window, int x, int y)
 	{
+		SDL.SDL_SetWindowPosition(window.Handle, x, y);
 	}
 
 	public Rectangle GetBounds(GameWindow window)
 	{
-		throw new PlatformNotSupportedException();
+		if ((bool)SDL.SDL_GetWindowPosition(window.Handle, out var x, out var y) && (bool)SDL.SDL_GetWindowSize(window.Handle, out var w, out var h))
+		{
+			return new Rectangle(x, y, w, h);
+		}
+		return default(Rectangle);
 	}
 }
